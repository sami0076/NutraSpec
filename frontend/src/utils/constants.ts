// API URL, risk levels, thresholds
// Always use /api prefix — in dev Vite proxies it, in prod Vercel rewrites proxy it.
// This avoids CORS and ngrok interstitial issues entirely.
export const API_BASE_URL = '/api';
