# FoodFinder.AI

**backend/** — Python, FastAPI. Scoring in `backend/scoring_engine/`.  
**web/** — TypeScript, Vite, React.

**Teammates:** see **STARTUP.md** to get the app running (clone, env, Docker or local).

Quick run: `cp backend/.env.example backend/.env` then `docker compose up --build`.  
Web → http://localhost:5173 · API → http://localhost:4000

ARCHITECTURE.md — flow and responsibilities.
